fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
numpy>=1.26
torch>=2.4
torchvision>=0.19
opencv-python-headless>=4.10
ffmpeg-python>=0.2

# EchoMimicV3 runtime (antgroup/echomimic_v3). Imported only inside the shelled
# inference process / preview wrapper, so the FastAPI app stays light.
# Mirrors the upstream requirements.txt (verified 2026-06).
# Pinned for torch 2.4.1 compatibility: diffusers >=0.34 ships a flash-attn-3
# custom-op whose schema torch 2.4 can't infer (import-time crash); transformers
# 5.x is likewise too new. These pins match EchoMimicV3's tested Wan2.1 stack.
diffusers==0.31.0
transformers==4.49.0
accelerate==0.34.2
omegaconf
safetensors
einops
librosa
soundfile
pyloudnorm
decord
moviepy==2.2.1
imageio[ffmpeg]
imageio[pyav]
av
Pillow
scikit-image
timm
tomesd
torchdiffeq
torchsde
SentencePiece
ftfy
func_timeout
onnxruntime
# retina-face/tensorflow are intentionally omitted. Upstream's
# src.face_detect.get_mask_coord uses retinaface -> tensorflow.keras, but our
# vendored echomimic_preview_infer.py computes the IP-mask bbox with insightface
# instead (POD_SETUP.md §4b), and infer_flash.py needs no face detector. Keeping
# tensorflow==2.15 would force ml-dtypes~=0.2.0, which conflicts with onnx's
# ml-dtypes>=0.5.4 (insightface/onnx path) and breaks onnx import in the venv.
mmgp
