{
  "name": "@las/control-api",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "migrate:local": "wrangler d1 migrations apply las_db --local",
    "migrate:remote": "wrangler d1 migrations apply las_db --remote"
  },
  "dependencies": {
    "@las/protocol": "*",
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241004.0",
    "typescript": "^5.6.2",
    "vitest": "^2.1.2",
    "wrangler": "^3.80.0"
  }
}
