# Body animation clips

Place retargeted glTF clips here:

- `M_Explain.glb`
- `M_LeanIn.glb`
- `M_Nod.glb`

Author from Mixamo FBX → Blender retarget to match `ada.glb` skeleton, export as
glTF binary. When clips are missing, set `MONTAGE_MODE=procedural` (default).
