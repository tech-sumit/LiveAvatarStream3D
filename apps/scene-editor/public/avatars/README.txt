Bundled preview models (Three.js examples, MIT):

- michelle.glb — default skinned humanoid preview + idle animation
- xbot.glb — robot humanoid with locomotion clips

Production render avatar (not in git — ~28MB meshopt):

- ada.glb — copy from H100 pod:
  services/engine-three/assets/avatars/ada.glb
  → apps/scene-editor/public/avatars/ada.glb

Additional GLBs: drop into this folder and set avatarId in Inspector, or import
props via Scene → GLB.
