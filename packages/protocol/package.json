{
  "name": "@las/protocol",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "npm run build --workspace @las/performer-core && tsc -p tsconfig.json",
    "typecheck": "npm run build --workspace @las/performer-core && tsc -p tsconfig.json --noEmit",
    "schema": "tsx scripts/gen-schema.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@las/performer-core": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.2",
    "zod-to-json-schema": "^3.23.3"
  }
}
